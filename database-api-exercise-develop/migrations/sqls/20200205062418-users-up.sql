CREATE TABLE IF NOT EXISTS Users (id   SERIAL , username VARCHAR(255), password VARCHAR(255), role_id INTEGER DEFAULT 2 REFERENCES Roles (id) ON DELETE CASCADE ON UPDATE CASCADE, createdAt TIMESTAMP NOT NULL DEFAULT NOW(), inactive BOOLEAN DEFAULT false, updatedAt TIMESTAMP NOT NULL DEFAULT NOW(),UNIQUE (username), PRIMARY KEY (id));